import LoginForm from '@renderer/login'

function HomePage() {
  return (
    <div className="w-full h-screen flex items-center justify-center">
      <LoginForm />
    </div>
  )
}

export default HomePage
